## Summary

Demonstrate manual composite tree construction with `CompositeTreeBuilder<T>` in the Composite sample (alongside existing catalog `BuildRoot()` demo).

## Acceptance criteria

- [ ] `Composite.Sample` includes manual `Leaf` / `Branch` / `Build()` walkthrough
- [ ] Update `docs/Composite.md` to reference the manual section

## Module

`samples/` + `docs/`
