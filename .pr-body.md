## Summary

- Adds `samples/DependencyInjection.Sample` demonstrating generated `RegisterDi` for Strategy, Factory, and Handler pipelines.
- Wires the sample into `DesignPatterns.slnx` and documents it in README / DEVELOPMENT.

Closes #5

## Test plan

- [x] `dotnet build` + `dotnet run` on `DependencyInjection.Sample`
- [ ] CI `BuildSamples` green
