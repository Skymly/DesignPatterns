## Summary

Add a CodeFix for DP024 that inserts `[HandlerOrder]` on unregistered `IHandler<TContext>` implementations.

## Acceptance criteria

- [ ] `AddHandlerOrderCodeFixProvider` fixes DP024 with `[HandlerOrder(order, typeof(TContext))]`
- [ ] Tests in `DesignPatterns.Analyzers.Tests`
- [ ] Document in `docs/ChainOfResponsibility.md`

## Module

`DesignPatterns.CodeFixes/` + `tests/DesignPatterns.Analyzers.Tests/` + `docs/`
